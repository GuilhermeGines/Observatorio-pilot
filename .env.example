# Optional. Prefer the operating-system credential store via Settings.
# Never commit your actual key. This file is documentation, not loaded automatically.
OPENAI_API_KEY=
# Default local data folder is ./data; use an absolute path to relocate it.
OBS_DATA_DIR=
